# Excel_Sheetdock
A free Excel add-in that adds two features Microsoft left out: a panel to click-jump between worksheets, and a live Sum / Average / Count / Min / Max strip on the status bar. Drag any stat into a cell to insert the matching formula.
